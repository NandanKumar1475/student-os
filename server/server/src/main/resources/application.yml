server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_os
    username: root
    password: Nandan@123!@#
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

app:
  jwt:
    secret: mySecretKeyForJWTTokenGenerationStudentOS2025MakeItLong
    expiration: 86400000